# Robot src_v2 Next Steps

1. Make `./dialtone.sh robot src_v2 test` fully green on WSL.
- Fix local browser attach instability in NAT mode.
- Default test flow to remote browser node when local attach is unavailable.
- Keep all `test/NN_*` steps passing, including `04_local_ui_mock_e2e`.

2. Complete mandatory UI E2E sign-off in `src/plugins/robot/src_v2/test/LLM_SIGNOFF.md`.
- E2E-1: local UI + mock `/natsws` producer.
- E2E-2: local UI + remote robot `/natsws`.
- Record commands, outputs, and artifacts for both.

3. Keep robot src_v2 runtime contract stable.
- `GET /health` returns `200 ok`.
- `GET /api/init` exposes `/natsws`.
- `GET /stream` behavior remains compatible.
- `/natsws` websocket upgrade + pub/sub works.

4. Finalize autoswap release safety for robot composition.
- Add checksum verification for downloaded release artifacts before activate.
- Keep staging behavior for binaries + `ui/dist` + wlan bundle.
- Keep activation swap behavior deterministic with rollback to previous known-good.

5. Validate side-by-side rollout gates before default flip.
- Run `src_v1` and `src_v2` fallback checks.
- Verify rover path and tunnel path stay functional.
- Document release gate pass/fail and rollback pointer.

6. Keep publish path release-aware for all required assets.
- `robot src_v2 publish` must skip already-uploaded assets.
- Upload only missing assets for all target OS/arch builds.
- Ensure required binaries and `robot_src_v2_ui_dist-<os>-<arch>.tar.gz` are present.

7. Keep WLAN driver workflow reproducible.
- Preserve compile/install instructions and patch workflow in `src/plugins/wlan/README.md`.
- Keep manifest expectation scoped to driver workflow artifacts only.

8. After all above are green, flip default robot composition to `src_v2`.
- Update docs and operator workflow commands.
- Keep `src_v1` fallback available until stable in production.
