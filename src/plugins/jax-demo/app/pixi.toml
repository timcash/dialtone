[workspace]
name = "jax-demo"
version = "0.1.0"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = "3.11.*"
numpy = "*"
cuda-nvcc = "*"

[pypi-dependencies]
jax = { extras = ["cuda12"] }

[tasks]
start = "python main.py"
