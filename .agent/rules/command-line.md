---
trigger: always_on
---

# Command Line Rules
1. DO NOT run multiple commands on the same line with `&&` or `;`
2. ALWAYS run commands on separate lines one at a time
3. this makes it easier to debug and understand the output of each command