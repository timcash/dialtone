---
trigger: always_on
---

Always use `dialtone.sh <command>` to run commands while developing. It makes sure to use the correct dependencies

Using `go` `npm` directly may not work as this repo uses a local install of those dependencies